## Includes Folder

This folder contains static snippets of HTML that are included via Flask.
Everything else in templates folder besides these is created by running `npm run build` in the `react-frontend` folder.
