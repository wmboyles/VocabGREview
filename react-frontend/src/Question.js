function Question() {
  return (
    <div class="jumbotron jumbotron-fluid">
      <div class="container">
        /** TODO Get question, answer choices and format them. Will likely have
        answer choice object that is repeated. */
      </div>
    </div>
  );
}

export default Question;
